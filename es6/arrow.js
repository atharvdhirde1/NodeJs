function add(a, b) {
    return a + b;
  }
  
  // Using an arrow function expression
  const addArrow = (a, b) => a + b;
  
  console.log(add(2, 3)); // logs 5
  console.log(addArrow(2, 2)); // logs 4